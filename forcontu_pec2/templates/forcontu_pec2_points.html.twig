{#
/**
 * @file forcontu_pec2_points.html.twig
 * Default theme implementation to present Points data.
 *
 * This template is used when viewing Points pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_forcontu_pec2_points()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('forcontu_pec2_points') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
